import { createSignal } from "./createSignal";
function createRef(e = null) {
  const [r, t] = createSignal({ current: e });
  return {
    get current() {
      return r().current;
    },
    set current(e) {
      t({ current: e });
    },
  };
}
export { createRef };
